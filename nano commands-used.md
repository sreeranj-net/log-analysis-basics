# Commands Used

The following Linux commands were used during log collection and analysis:

- journalctl -u ssh
- journalctl _COMM=sshd
- grep "Failed password" auth.log
- grep "Invalid user" auth.log
